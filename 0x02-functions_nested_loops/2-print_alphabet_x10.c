#include "main.h"

/**
 * print_alphabet_x10 - writes the alphabet on the stdout x10
 */

void print_alphabet_x10(void)
{
	int xten;
	char la;

	for (xten = 0; xten <= 9; xten++)
	{
		for (la = 'a'; la <= la; la++)
			_putchar(la);
		_putchar('\n');
	}
}

